# This file marks `backend/routers/` as a Python package.
# Import routers here if you want to expose them from a single import site.
# For now we keep it empty — main.py imports each router directly.
