{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "frontend",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["--prefix", "frontend", "run", "dev"],
      "port": 5173,
      "autoPort": false
    },
    {
      "name": "backend",
      "runtimeExecutable": "/Users/rorkpashagmail.com/Documents/JobifAI/.claude/worktrees/sad-brattain/.venv/bin/python3",
      "runtimeArgs": [
        "-c",
        "import os, sys; wd='/Users/rorkpashagmail.com/Documents/JobifAI/.claude/worktrees/sad-brattain'; sys.path.insert(0, wd); os.chdir(wd); [os.environ.setdefault(k.strip(), v.strip()) for line in open(wd+'/.env') if '=' in line and not line.startswith('#') for k, v in [line.strip().split('=', 1)]]; os.execv(wd+'/.venv/bin/uvicorn', ['uvicorn', 'backend.main:app', '--reload', '--host', '0.0.0.0', '--port', '8000'])"
      ],
      "port": 8000,
      "autoPort": false
    }
  ]
}
